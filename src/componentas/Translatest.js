import React from "react";
import translatte from translatte;

const Translator = (props) => {
    return (
        <div className='col'>
            <h1>aberm {props.texte}</h1>
        </div>
    )
}

import default Translator;